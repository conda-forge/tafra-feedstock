{% set name = "tafra" %}
{% set version = "1.1.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: d1ee96903611efe84819383dcf4579bc1eacb4c3bcf280d697876cc25d00c374

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python {{ python_min }}
    - numpy >=1.17
    - typing-extensions
  run:
    - numpy >=1.17
    - python >={{ python_min }}
    - typing-extensions

test:
  source_files:
    - test
  imports:
    - tafra
    - test
  commands:
    - pip check
    - pytest --cov=tafra --cov-report=term-missing --hypothesis-show-statistics -v test
  requires:
    - pip
    - pytest
    - hypothesis
    - pytest-cov
    - pandas
    - python {{ python_min }}

about:
  home: https://github.com/petbox-dev/tafra
  summary: 'Tafra: innards of a dataframe'
  license: MIT
  license_file: LICENSE
  description: A minimalist typed dataframe with functional programming concepts.
  doc_url: https://tafra.readthedocs.io
  dev_url: https://github.com/petbox-dev/tafra


extra:
  recipe-maintainers:
    - dsfulf
